# Direct dependencies (human-edited source).
# Regenerate the hash-locked requirements.txt after editing this file:
#   docker run --rm -v "$PWD:/w" -w /w python:3.14-alpine sh -c \
#     "pip install pip-tools && pip-compile --generate-hashes --output-file=requirements.txt requirements.in"
Flask==3.1.3
cryptography==50.0.1
SQLAlchemy==2.0.54
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.3.0
Flask-Login==0.6.3
python-dotenv==1.2.3
gunicorn==26.2.0
ldap3==2.9.1
# A1 SoftHSM/PKCS#11 key backend (only used when KEY_BACKEND=softhsm)
python-pkcs11==0.10.0
asn1crypto==1.5.1
# DoS-1: rate limiting is enabled by default (RATE_LIMIT_ENABLED=true)
Flask-Limiter==4.1.1
# Prometheus /metrics endpoint (only imported when METRICS_ENABLED=true)
prometheus-client==0.26.0
# F13 TOTP second factor: QR code for the enrolment page (pure Python, no dependencies)
segno==1.6.6

# 3.6.0 (dns-01 validation for the ACME server): TXT lookups, CNAME following.
dnspython==2.8.0
